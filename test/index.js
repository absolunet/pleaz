//--------------------------------------------------------
//-- Node IoC - Test
//--------------------------------------------------------
'use strict';

const { tester } = require('@absolunet/tester');

tester.init({
	repositoryType: 'single-package',
	packageType:    'ioc'
});
